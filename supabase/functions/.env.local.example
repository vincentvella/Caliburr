# Local config for Supabase Edge Functions.
# Copy to .env.local and fill in your values.
# This file is not used at runtime — secrets are set via `bunx supabase secrets set`.

# RevenueCat webhook shared secret (from RevenueCat dashboard → Integrations → Webhooks)
REVENUECAT_WEBHOOK_SECRET=your_secret_here
